node_modules/
build/
dist/
dist-presentation/
coverage/
.DS_Store
*.log
.env*
!.env.example
!.env.production

# Firebase rules (deploy via firebase CLI, not git)
firestore.rules
# firebase-applet-config.json is tracked — client config is not a secret

# Dev tool artifacts
.claude/worktrees/
.playwright-mcp/
.superpowers/
.wrangler/

# Admin dashboard screenshot harness — build + image artifacts (regenerable
# via `vite build --config snapshots/vite.config.ts` + `node snapshots/shoot.mjs`)
/snapshots-dist/
/snapshots-out/
test-results/

# Backup files
*.backup
*.backup_*
*.bak
*.orig
test-ocr-flow.js
venv_ocr/
__pycache__/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
test-results/
playwright-report/

tmp/

tmp/

temp-audio/

# Lemma generator artefacts (review-only, merged into vocabulary-hebrew.ts by hand)
.cache/hebrew-lemmas/
src/data/vocabulary-hebrew.generated.ts
docs/mockups/shots/
